const initState = {
  isLogin: false,
  isReset: false,
  token: "",
  user: "",
};

export const authReducer = (state = initState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
